---
// src/pages/index.astro

const gamesList = [
  {
    title: "[00] Soloverse [Universal Game Notes]",
    folder: "00-soloverse-universal-notes",
    boxColor: "#b2b2b2", // Stone box theme
    files: [
      { name: "📖 Universal Game Notes", file: "/00-a-universal-game-notes", version: "4-0-0", isWebPage: true },
      { name: "🃏 Variations Rulebook", file: "/00-b-variations-rulebook", version: "4-0-0", isWebPage: true },
      { name: "🕹️ Patch Notes", file: "/00-c-patch-notes", version: "4-0-0", isWebPage: true }
    ]
  },
  {
    title: "[00] Soloverse [Contact Us]",
    folder: "00-soloverse-universal-notes",
    boxColor: "#b2b2b2", 
    files: [
      { name: "Join Our Discord", file: "https://discord.gg/ShuxrtsX6s", version: "1-0-0", isWebPage: false, iconType: "discord" },
      { name: "Facebook Page", file: "https://facebook.com/soloversegame", version: "1-0-0", isWebPage: false, iconType: "facebook" },
      { name: "Instagram", file: "https://instagram.com/soloversegame", version: "1-0-0", isWebPage: false, iconType: "instagram" },
      { name: "✉ Email", file: "mailto:soloversegame@gmail.com", version: "1-0-0", isWebPage: false }
    ]
  }
  {
    title: "[01] Soloverse - Crazy 8's",
    folder: "01-soloverse-crazy-8s",
    boxColor: "#0000ff", // Steel box theme
    files: [
      { name: "📖 Game Rulebook", file: "/01-a-crazy-8s-game-rulebook", version: "15-0-0", isWebPage: true },
      { name: "🃏 Chaos Card Definitions", file: "/01-b-crazy-8s-definitions-rulebook", version: "14-0-0", isWebPage: true },
      { name: "🕹️ Solo Mode Rules", file: "01-c-soloverse-crazy-8s-solo-mode-x-bot-player-rulebook", version: "1-0-0" },
      { name: "🏆 Achievements List", file: "01-d-soloverse-crazy-8s-achievements-list", version: "1-0-0" }
    ]
  },
  {
    title: "[02] Soloverse - Crazy 8's [Utter Chaos]",
    folder: "02-soloverse-crazy-8s-utter-chaos",
    boxColor: "#0ed145", // Toxin box theme
    files: [
      { name: "📖 Game Rulebook", file: "/02-a-crazy-8s-utter-chaos-game-rulebook", version: "4-0-0", isWebPage: true },
      { name: "🃏 Chaos Card Definitions", file: "02-b-soloverse-crazy-8s-utter-chaos-definitions-rulebook", version: "2-0-0" }
    ]
  },
  {
    title: "[03] Soloverse - Karma",
    folder: "03-soloverse-karma",
    boxColor: "#fff200", // Stinger box theme
    files: [
      { name: "📖 Game Rulebook", file: "/03-a-karma-game-rulebook", version: "4-0-0", isWebPage: true },
      { name: "🃏 Chaos Card Definitions", file: "03-b-soloverse-karma-definitions-rulebook", version: "2-0-0" }
    ]
  },
  {
    title: "[04] Soloverse - Cheat",
    folder: "04-soloverse-cheat",
    boxColor: "#ff7f27", // Carbon box theme
    files: [
      { name: "📖 Game Rulebook", file: "/04-a-cheat-game-rulebook", version: "4-0-0", isWebPage: true },
      { name: "🃏 Chaos Card Definitions", file: "04-b-soloverse-cheat-definitions-rulebook", version: "2-0-0" }
    ]
  },
  {
    title: "[05] Soloverse - Battle Solitaire",
    folder: "05-soloverse-battle-solitaire",
    boxColor: "#b97a56", // Oak box theme
    files: [
      { name: "📖 Game Rulebook", file: "/05-a-battle-solitaire-game-rulebook", version: "4-0-0", isWebPage: true },
      { name: "🃏 Chaos Card Definitions", file: "05-b-soloverse-battle-solitaire-definitions-rulebook", version: "2-0-0" }
    ]
  },
  {
    title: "[06] Soloverse -  Deck-Builder",
    folder: "06-soloverse-deck-builder",
    boxColor: "#ec1c24", // Venom box theme
    files: [
      { name: "📖 Game Rulebook", file: "/06-a-deck-builder-game-rulebook", version: "4-0-0", isWebPage: true },
      { name: "🃏 Chaos Card Definitions", file: "06-b-soloverse-deck-builder-definitions-rulebook", version: "2-0-0" }
    ]
  },
  {
    title: "[07] Soloverse - Nominations",
    folder: "07-soloverse-nominations",
    boxColor: "#9d00ff", // Poison box theme
    files: [
      { name: "📖 Game Rulebook", file: "/07-a-nominations-game-rulebook", version: "3-0-0", isWebPage: true },
      { name: "🃏 Chaos Card Definitions", file: "07-b-soloverse-nominations-definitions-rulebook", version: "2-0-0" }
    ]
  }
];
const upcomingGames = [
  { title: "08 Soloverse - 500" },
  { title: "09 Soloverse - Poker" },
  { title: "10 Soloverse - Donkey" },
  { title: "11 Soloverse - Go Fish" },
  { title: "12 Soloverse - Sevens" },
  { title: "13 Soloverse - Snap" }
];
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

	<link rel="stylesheet" href="https://cloudflare.com">

    <!-- Permanent Cache-Busting Network Headers -->
    <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate" />
    <meta http-equiv="Pragma" content="no-cache" />
    <meta http-equiv="Expires" content="0" />

    <title>Soloverse Rulebook Hub</title>

    <!-- 1. Tell the phone browser exactly where your live app blueprint sits -->
    <link rel="manifest" href="/manifest.webmanifest" />

    <!-- 2. Force the mobile address bar and theme layers to adapt to your dark game profile -->
    <meta name="theme-color" content="#0f1015" />

    <style>
        /* ==========================================================================
           AUTOMATED TABLET & WIDESCREEN RESPONSIVE ENGINE
           ========================================================================== */

        /* Automatically expands text spreads when tablets are flipped horizontally */
        @media screen and (min-width: 768px) and (orientation: landscape) {
            body {
                max-width: 960px; /* Expands reading column limits for a comfortable landscape view */
                margin: 40px auto;
                padding: 0 30px;
            }

            .rules-box {
                padding: 32px;
                margin: 24px 0;
            }
        }

        /* Large Screen Optimizations (Large Tablets, Monitors, and Laptops) */
        @media screen and (min-width: 1024px) {
            body {
                max-width: 1050px; /* Sweeps columns out nicely on large display panels */
            }
        }
		
		/* PWA Installation Overlay Banner Layout */
		.pwa-install-banner {
			position: fixed;
			bottom: 20px;
			left: 50%;
			transform: translateX(-50%);
			width: calc(100% - 40px);
			max-width: 500px;
			background: var(--bg-card);
			border: 1px solid var(--text-divider);
			box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.4);
			border-radius: 12px;
			padding: 16px;
			z-index: 9999;
			display: none; /* Controlled dynamically via runtime scripts */
			flex-direction: column;
			gap: 12px;
			transition: opacity 0.3s ease;
		}

		.pwa-header {
			display: flex;
			align-items: center;
			justify-content: space-between;
		}

		.pwa-title {
			font-weight: 700;
			color: var(--text-primary);
			font-size: 1rem;
		}

		.pwa-close-btn {
			background: none;
			border: none;
			color: var(--text-muted);
			font-size: 1.2rem;
			cursor: pointer;
			padding: 0 4px;
		}

		.pwa-body {
			font-size: 0.9rem;
			color: var(--text-secondary);
			line-height: 1.4;
		}

		.pwa-action-row {
			display: flex;
			justify-content: flex-end;
			gap: 8px;
			margin-top: 4px;
		}

		.pwa-btn {
			padding: 8px 16px;
			border-radius: 6px;
			font-size: 0.85rem;
			font-weight: 600;
			cursor: pointer;
			border: none;
		}

		.pwa-btn-primary {
			background: var(--color-link);
			color: #ffffff;
		}

		.pwa-btn-secondary {
			background: var(--bg-main-box);
			border: 1px solid var(--text-divider);
			color: var(--text-secondary);
		}

		/* Landscape-Specific Media Rules Override */
		@media (orientation: landscape) and (min-width: 768px) {
			body {
				max-width: 1000px; /* Expands rulebook layout limits automatically when flipped horizontally */
				margin: 20px auto;
				padding: 30px;
				display: grid;
				grid-template-columns: 1fr; /* Ensures container blocks adapt smoothly to the extra real estate */
			}

			.rules-box {
				padding: 32px; /* Amplifies grid spacing layouts comfortably for widescreen reading */
			}
		}
		
        /* 1. DEFINE YOUR REACTIVE COLOR MAP ARCHITECTURE */
        :root {
            --bg-main: #0f1015;
            --bg-card: #171922;
            --bg-contents: #1c1e27;
            --bg-link: #242732;
            --bg-link-hover: #3b82f6;
            --text-primary: #ffffff;
            --text-secondary: #f3f4f6;
            --text-muted: #9ca3af;
            --text-divider: #2d3139;
            --shadow-card: rgba(0, 0, 0, 0.5);
        }

        /* 2. AUTOMATIC LIGHT MODE OVERRIDE DATA VALUES */
        :root[data-theme="light"] {
            --bg-main: #f4f5f7;
            --bg-card: #ffffff;
            --bg-contents: #f9fafb;
            --bg-link: #f3f4f6;
            --bg-link-hover: #e5e7eb;
            --text-primary: #111216;
            --text-secondary: #1f2937;
            --text-muted: #4b5563;
            --text-divider: #d1d5db;
            --shadow-card: rgba(0, 0, 0, 0.1);
        }

        body {
            font-family: system-ui, -apple-system, sans-serif;
            background: var(--bg-main);
            color: var(--text-secondary);
            max-width: 900px;
            margin: 40px auto;
            padding: 20px;
            transition: background 0.3s, color 0.3s;
        }
        h1 {
            text-align: center;
            color: var(--text-primary);
            font-size: 2.5rem;
            margin-bottom: 5px;
            font-weight: 800;
            letter-spacing: -0.05em;
        }
        .subtitle {
            text-align: center;
            color: var(--text-muted);
            margin-bottom: 50px;
        }
        
        /* 1. BUTTON HOVER CONTAINER FLEXBOX LAYOUT (Add this above your button code) */
        .theme-toggle-container {
            display: flex;
            justify-content: flex-start;
            align-items: center;
            gap: 10px;
            margin-bottom: 25px;
        }

        /* 2. THE CORE BUTTON DESIGN (Keep your custom pill configurations) */
        .theme-btn {
            background: var(--bg-card);
            color: var(--text-primary);
            border: 1px solid var(--text-divider);
            padding: 10px 16px;
            border-radius: 50px; /* Keeps your premium pill design */
            font-weight: 700;
            cursor: pointer;
            font-size: 0.9rem;
            box-shadow: 0 4px 6px -1px var(--shadow-card);
            display: inline-flex; /* Ensures text and emojis center perfectly together */
            align-items: center;
            gap: 8px;
            transition: background 0.2s, color 0.2s, transform 0.1s, border-color 0.2s;
        }

        /* Adds a subtle layout response highlight when hovering over buttons */
        .theme-btn:hover {
            border-color: var(--color-link);
            background: var(--bg-main-box);
        }

        /* 3. ACTIVE CLICK SPRING ANIMATION (Keep this completely intact!) */
        .theme-btn:active { 
            transform: scale(0.97); /* Keeps your tactile push spring effect on click */
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
            gap: 30px;
        }
        .game-card {
            background: var(--bg-card);
            border-radius: 16px;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            box-shadow: 0 10px 25px -5px var(--shadow-card), 0 8px 10px -6px var(--shadow-card);
            transition: transform 0.2s cubic-bezier(0.4, 0, 0.2, 1), box-shadow 0.2s;
        }
        .game-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 20px 25px -5px var(--shadow-card), 0 10px 10px -5px var(--shadow-card);
        }
        .game-header {
            padding: 24px 20px;
            position: relative;
        }
        .game-header::after {
            content: '';
            position: absolute;
            inset: 0;
            background: linear-gradient(to bottom, rgba(255,255,255,0.12), rgba(0,0,0,0.35));
        }
        .game-title {
            position: relative;
            z-index: 10;
            margin: 0;
            font-size: 1.15rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 0.03em;
        }
        .box-contents {
            padding: 20px;
            background: var(--bg-contents);
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            gap: 12px;
            border-top: 1px solid var(--text-divider);
        }
        .links-list {
            display: flex;
            flex-direction: column;
            gap: 8px;
            margin: 0;
            padding: 0;
            list-style: none;
        }
        .rule-link {
            display: flex;
            align-items: center;
            background: var(--bg-link);
            color: var(--text-muted);
            text-decoration: none;
            padding: 12px 14px;
            border-radius: 8px;
            font-weight: 600;
            font-size: 0.9rem;
            border: 1px solid var(--text-divider);
            transition: background 0.15s, color 0.15s, border-color 0.15s;
        }
        .rule-link:hover {
            background: var(--bg-link-hover);
            color: var(--text-primary);
        }
        .section-divider {
            grid-column: 1 / -1;
            margin-top: 60px;
            margin-bottom: 20px;
            border-bottom: 1px dashed var(--text-divider);
            padding-bottom: 15px;
            color: var(--text-muted);
            font-size: 1.25rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }
        .blueprint-card {
            background: var(--bg-card);
            opacity: 0.7;
            border: 2px dashed var(--text-divider);
            border-radius: 16px;
            padding: 24px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            min-height: 120px;
            position: relative;
            overflow: hidden;
        }
        .blueprint-title {
            margin: 0;
            font-size: 1rem;
            font-weight: 700;
            color: var(--text-muted);
        }
        .status-badge {
            align-self: flex-start;
            background: var(--bg-link);
            color: var(--text-muted);
            font-size: 0.7rem;
            font-weight: 700;
            padding: 4px 8px;
            border-radius: 4px;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            margin-top: 15px;
        }
    </style>

</head>
<body>

    <!-- THE REACTIVE THEME & ROTATION OPTION INTERFACE CONTROLLER BUTTONS -->
    <div class="theme-toggle-container">
        <button id="theme-toggle-btn" class="theme-btn">☀️ Light Mode</button>
    </div>
	
    <h1>Soloverse Rulebook Hub</h1>
    <p class="subtitle">Select a Game Variant below to stream live, iOS-friendly interactive document layers.</p>

    <div class="grid">
        {gamesList.map((game) => {
            const titleColor = game.boxColor === "#fff200" ? "#111216" : "#ffffff";
            const textShadowRule = game.boxColor === "#fff200" ? "none" : "0 2px 4px rgba(0,0,0,0.6)";

            return (
                <div class="game-card">
                    <div class="game-header" style={`background-color: ${game.boxColor}; border-bottom: 4px solid rgba(0,0,0,0.25);`}>
                        <h2 class="game-title" style={`color: ${titleColor}; text-shadow: ${textShadowRule};`}>
                            {game.title}
                        </h2>
                    </div>
                    
					{/* RESTORED: Vital container hooks to stop compiler errors */}
					<div className="box-contents">
						<nav className="links-list">
							{game.files.map((item, key) => {
								// 1. PROTOCOL CHECK: Identify mailto or external absolute links
									const isExternalProtocol = item.file.startsWith('mailto:') || item.file.startsWith('http');
	
								let finalUrl;

									if (isExternalProtocol) {
									// If it's an email link or web protocol, pass it raw
									finalUrl = item.file;
								} else if (item.isWebPage) {
									// 2. SMART WEB PAGE OVERRIDE: If the file has 'isWebPage: true', use your clean direct URL string instead!
										finalUrl = item.file;
								} else {
									// 3. DEFAULT PDF ROUTE: If it's a standard PDF file, build the hidden hyphenated array path
									const folderPathArray = ["", "rulebooks", game.folder, item.file + "-v" + item.version + ".pdf"];
									finalUrl = folderPathArray.join('/');
								}

								return (
									<div key={key} className="link-button-wrapper" style={{ width: "100%" }}>
										<a 
											href={finalUrl} 
											className="rule-link universal-pdf-trigger" 
											style={{
												borderLeft: `3px solid ${game.boxColor}`, 
												display: "flex", 
												alignItems: "center", // Keeps vector icon inline and centered with text labels
												width: "100%"
											}}
												target={isExternalProtocol && !item.file.startsWith('mailto:') ? "_blank" : undefined}
											rel={isExternalProtocol ? "noopener noreferrer" : undefined}
										>
											{/* 4. DYNAMIC INLINE ICON CHECK */}
											{item.iconType === 'discord' && (
												<svg width="16" height="16" viewBox="0 0 127.14 96.36" fill="currentColor" style={{ marginRight: '8px', display: 'inline-block', shrink: 0 }}><path d="M107.7,8.07A105.15,105.15,0,0,0,77.26,0a77.19,77.19,0,0,0-3.3,6.83A96.67,96.67,0,0,0,53.22,6.83,77.19,77.19,0,0,0,49.88,0,105.15,105.15,0,0,0,19.44,8.07C3.66,31.58-1.86,54.65,1,77.53A105.73,105.73,0,0,0,32,96.36a74.37,74.37,0,0,0,6.66-10.85A68.52,68.52,0,0,1,28.32,80.3c.87-.64,1.72-1.31,2.53-2a75.47,75.47,0,0,0,65.46,0c.81.69,1.66,1.36,2.53,2a68.43,68.43,0,0,1-10.35,5.21,74.52,74.52,0,0,0,6.66,10.85,105.47,105.47,0,0,0,31.07-18.83C129.87,50.22,123.81,27.35,107.7,8.07ZM42.45,65.69C36.18,65.69,31,60,31,53S36.18,40.36,42.45,40.36,53.83,46,53.83,53,48.72,65.69,42.45,65.69Zm42.24,0C78.41,65.69,73.24,60,73.24,53S78.41,40.36,84.69,40.36,96.07,46,96.07,53,91,65.69,84.69,65.69Z"/></svg>
											)}
											{item.iconType === 'facebook' && (
												<svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor" style={{ marginRight: '8px', display: 'inline-block', shrink: 0 }}><path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/></svg>
											)}
											{item.iconType === 'instagram' && (
												<svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor" style={{ marginRight: '8px', display: 'inline-block', shrink: 0 }}><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.051.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 100 12.324 6.162 6.162 0 000-12.324zM12 16a4 4 0 110-8 4 4 0 010 8zm6.406-11.845a1.44 1.44 0 100 2.881 1.44 1.44 0 000-2.881z"/></svg>
											)}

											{item.name} 
                        
											<span style={{ marginLeft: "auto", fontSize: "0.75rem", opacity: 0.6, fontWeight: "normal", marginRight: "10px" }}>
												v{item.version.replaceAll('-', '.')}
											</span>
										</a>
									</div>
								);
							})}
						</nav>
					</div>
                </div>
            );
        })}
    
        <div class="section-divider">
            🛠️ In Development Blueprint Stack ({upcomingGames.length} Variants Pipeline)
        </div>

        {upcomingGames.map((upcoming) => (
            <div class="blueprint-card">
                <h2 class="blueprint-title">{upcoming.title}</h2>
                <div class="status-badge">📦 Under Construction</div>
            </div>
        ))}
    </div>

    <!-- Smart PWA Installation Banner Overlay Component -->
    <div id="pwa-banner" class="pwa-install-banner">
        <div class="pwa-header">
            <div class="pwa-title">📱 Add Soloverse to Home Screen</div>
            <button id="pwa-close" class="pwa-close-btn" aria-label="Close popup">×</button>
        </div>
        <div id="pwa-instructions" class="pwa-body">
            <!-- Content will be auto-generated by the script layer based on device profiling -->
        </div>
        <div class="pwa-action-row">
            <button id="pwa-dismiss" class="pwa-btn pwa-btn-secondary">Not Now</button>
            <button id="pwa-action-btn" class="pwa-btn pwa-btn-primary" style="display: none;">Install App</button>
        </div>
    </div>

    <!-- Unified Soloverse Platform Engine: Handles Service Workers, Auto-Theme Detection, and UI Controls -->
    <script is:inline>
        (function() {
            // ==========================================
            // 1. SERVICE WORKER REGISTER & RESET CONTROLS
            // ==========================================
            if ('serviceWorker' in navigator) {
                // Register the background service worker architecture on initial load
                window.addEventListener('load', () => {
                    navigator.serviceWorker.register('/sw.js');
                });

                // Force background service worker registrations to immediately pull fresh server updates from Vercel
                navigator.serviceWorker.getRegistrations().then(function(registrations) {
                    for (let registration of registrations) {
                        registration.update(); 
                    }
                });
            }

            // ==========================================
            // 2. AUTOMATED THEME EVALUATION OR OVERRIDE
            // ==========================================
            const savedTheme = localStorage.getItem('soloverse-theme');
            const devicePrefersDarkMode = window.matchMedia('(prefers-color-scheme: dark)').matches;
            
            let activeTheme = 'dark'; // Base anchor default fallback

            if (savedTheme) {
                // Priority 1: Honor the explicit button selections saved in device memory
                activeTheme = savedTheme;
            } else {
                // Priority 2: Auto-detect and adopt the system mode configuration of the Android/iOS device
                activeTheme = devicePrefersDarkMode ? 'dark' : 'light';
            }

            // Mount evaluated selection instantly to prevent blinding screen flicker loops
            document.documentElement.setAttribute('data-theme', activeTheme);

            // ==========================================
            // 3. UI BUTTON CLICK RUNTIME MANAGEMENT
            // ==========================================
            window.addEventListener('DOMContentLoaded', () => {
                const themeBtn = document.getElementById('theme-toggle-btn');
                if (!themeBtn) return; // Operational guard to stop compiler crashes if the button hasn't loaded yet

                // Initialize the structural text on your shelf button to match active runtime metrics
                updateButtonUI(activeTheme);

                // Setup the mutation event tracker for deliberate player clicks
                themeBtn.addEventListener('click', () => {
                    const currentTheme = document.documentElement.getAttribute('data-theme');
                    const targetTheme = currentTheme === 'dark' ? 'light' : 'dark';
                    
                    document.documentElement.setAttribute('data-theme', targetTheme);
                    localStorage.setItem('soloverse-theme', targetTheme);
                    updateButtonUI(targetTheme);
                });

                function updateButtonUI(theme) {
                    themeBtn.innerText = theme === 'dark' ? '☀️ Light Mode' : '🌙 Dark Mode';
                }
            });

            // ==========================================
            // 4. AUTOMATED PWA INSTALLATION INTERCEPTOR
            // ==========================================
            window.addEventListener('DOMContentLoaded', () => {
                const banner = document.getElementById('pwa-banner');
                const bodyText = document.getElementById('pwa-instructions');
                const closeCross = document.getElementById('pwa-close');
                const dismissBtn = document.getElementById('pwa-dismiss');
                const actionBtn = document.getElementById('pwa-action-btn');

                if (!banner) return; // Structural guard to prevent console errors

                // Verify if the user has already explicitly closed this banner in the past
                if (localStorage.getItem('pwa-banner-dismissed') === 'true') return;

                // Device Profiling Flags
                const isIOS = /iPad|iPhone|iPod/.test(navigator.userAgent) && !window.MSStream;
                const isStandalone = window.matchMedia('(display-mode: standalone)').matches || window.navigator.standalone;

                // Stop execution entirely if the player is already using the installed home screen app version
                if (isStandalone) return;

                let deferredPrompt = null;

                if (isIOS) {
                    // Scenario A: Apple iOS Platform Profiler (Safari Directions Injection)
                    bodyText.innerHTML = "Access the Soloverse platform like a native mobile app! Tap the <strong>Share</strong> button in Safari (square with an up arrow) and select <strong>Add to Home Screen</strong>.";
                    banner.style.display = 'flex';
                } else {
                    // Scenario B: Android / Google Chrome Engine Listener
                    window.addEventListener('beforeinstallprompt', (e) => {
                        // Intercept Chrome's default browser overlay popup strip
                        e.preventDefault();
                        deferredPrompt = e;

                        bodyText.innerHTML = "Install the Soloverse library layout onto your device grid for lightning-fast book rendering and full offline table support.";
                        actionBtn.style.display = 'block';
                        banner.style.display = 'flex';
                    });
                }

                // Interactive Event Triggers for Dismissing or Running Installs
                const hideBanner = () => {
                    banner.style.opacity = '0';
                    setTimeout(() => { banner.style.display = 'none'; }, 300);
                };

                closeCross.addEventListener('click', hideBanner);
                dismissBtn.addEventListener('click', () => {
                    localStorage.setItem('pwa-banner-dismissed', 'true'); // Remembers dismissal setting forever
                    hideBanner();
                });

                actionBtn.addEventListener('click', async () => {
                    if (!deferredPrompt) return;
                    hideBanner();
                    deferredPrompt.prompt(); // Fires native Google Chrome install pipeline dialog layer
                    const { outcome } = await deferredPrompt.userChoice;
                    if (outcome === 'accepted') {
                        localStorage.setItem('pwa-banner-dismissed', 'true');
                    }
                    deferredPrompt = null;
                });
            });

            // ==========================================
            // 5. MANUAL CANVAS ROTATION OVERRIDE CONTROLS
            // ==========================================
            window.addEventListener('DOMContentLoaded', () => {
                const rotateBtn = document.getElementById('rotate-view-btn');
                if (!rotateBtn) return; // Operational guard to stop console errors if button is absent

                let currentRotateState = localStorage.getItem('soloverse-rotate-lock') || 'portrait';
                document.documentElement.setAttribute('data-rotate-override', currentRotateState);

                rotateBtn.addEventListener('click', () => {
                    const activeState = document.documentElement.getAttribute('data-rotate-override');
                    const targetState = activeState === 'landscape' ? 'portrait' : 'landscape';
                    
                    document.documentElement.setAttribute('data-rotate-override', targetState);
                    localStorage.setItem('soloverse-rotate-lock', targetState);
                });
            });

        })();
    </script>

</body>
</html>